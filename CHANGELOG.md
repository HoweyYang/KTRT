# KTRT 更新日志（Changelog）

## v0.1.3（2026-08-20）— 自定义查词典 + 单词笔记本 + 体验优化
- 自定义查词典：学习页新增入口，支持 ① 离线词典速查 ② 词在已导入词书时直接「收藏」③ 词不在任何词书时用 AI 按标准范式生成，加入默认「外部单词收藏册」（每满 50 词换一个 List）。
- 默认词书调整：「外部单词收藏册」成为默认书（空书也常驻）；GRE必背 不再默认导入；词库统一放仓库 `wordbooks/` 目录按需下载。
- 单词笔记本：每个词可写 Markdown 笔记（`#`/`##` 两级标题、`**加粗**`、`==高亮==`，无斜体），代码 / 展示双视图，展示层支持框选右键高亮 / 取消高亮；显式「保存」按钮，未保存草稿切换即丢弃；管理页支持「有笔记」筛选、按词书/List 清空笔记、导出笔记（词 + 笔记循环）。
- 导出优化：词汇导出先选范围（不熟悉 / 收藏 / 一并），再按词书与 List 过滤导出。
- UI 与交互：移除「熟悉」标记（学习页与管理页）；管理页单词默认折叠（点 List 三角展开）；移除冗余「刷新」按钮；刷新页面保持当前所在页；「收藏单词册」更名为「外部单词收藏册」。
- 数据整理：清理历史遗留的孤立词条（备份已归档到 E 盘）。

## v0.1.2（2026-08-19）— 深色蓝默认主题 + 语音设置 + 使用须知
- 默认界面主题改为「深色 · 蓝」（设置里仍可选 浅色 / 深色蓝 / 深色高对比）。
- 语音升级：英语支持 美音/英音 × 男声/女声 四选，法语支持 男声/女声；新增 语速 / 音调 / 音量 调节（edge-tts 与浏览器语音均生效）。
- 修复主词朗读多读「音标」的问题（TTS 文本不再拼接音标）；朗读自动剔除中文翻译与全角括号，避免英文语音读中文。
- 设置页新增「使用须知 · 避坑指南」：常见崩溃原因、备份与恢复建议。
- GRE必背同根词全面修正（本地词库）：按「同根词 = 与目标词共享同一词根」标准重排；同义词的派生词回迁至同义词列并标注为 `原词（变形）`（如 abridge（abridgement, abridger）），删除重复与无关词，不足 2 个的词由 AI 按新标准补全并清洗生造词；修正后 6174 词拥有 1-4 个可靠同根词。词库数据在本机 data/（不入库），Release 附件待后续发布时同步。
- 软件更新：设置页新增「检查补丁更新 / 检查版本更新」（GitHub Atom 源实现，无 API 限流），展示最新 commit 与 Release 及下载链接。
- AI 造句规则：提示词与单词无关时忽略提示词、基于该单词随机造句；提示词相关时允许使用同根词/词性变形（如 abbreviate → abbreviation）；前端高亮同步支持同根词与变形。
- 朗读优化：多条搭配/短语/同根词之间（；分隔）自动加入停顿，不再一口气读完；TTS 响应禁用缓存（no-store）并加时间戳参数，避免浏览器播放旧语音。
- 路线图更新：单词笔记本、软件更新入口（更新补丁/更新版本）、lite 版发布策略（无默认词书、资料库独立附件）、安装器三语（简中/繁中/英语）、README 重写计划；软著申请指南存放于本机 tools/（不入库）。

## v0.1.1b（2026-08-18）— 关键崩溃修复
- 修复启动弹窗（tkinter/Tcl）销毁时硬崩溃整个进程（tcl86t.dll、异常 0x80000003）导致服务中途退出、后续操作 failed to fetch 的问题。
- 启动弹窗改为独立子进程运行：其 Tcl 回收崩溃只影响弹窗本身，主服务不受影响；已运行时点击仍显示弹窗。
- 验证：主进程连续运行稳定、端口持续监听、导出/收藏/管理接口正常。
## v0.1.1（2026-08-18）— 词库增强 + 体验与性能优化

### 词库
- GRE必背（6519 词）：全部单词的「搭配」「短语」加入地道中文翻译（英文在前、中文在后）；
  2130 个动词补充常用短语动词（含中文释义，区分字面直译与真实含义）；12 条数量不匹配词条定向修正。
- 新增考研英语词库附件 `KAOYAN_Wordbook.xlsx`（考研英语词汇词根+联想记忆法，5905 词，52 个 List），与 GRE、雅思并列。
- 三本词库均作为 Release 独立附件，可按需下载导入。

### 导出词汇
- 管理页新增「导出词汇」：可按「不熟悉」「收藏」单独或一并导出；
  导出为 Excel，内容为每个词在词库中的完整一行（单词 / 音标 / 词性释义 / 搭配 / 短语 / 同义词 / 反义词 / 同根词 / List / 语言 / 单词书），可直接再次导入。

### 启动体验
- 双击启动不再弹出终端窗口（打包版 `--noconsole`，源码版提供 `KTRT.vbs` 无终端入口）。
- 新增启动预备弹窗：带球小人 + 绿色渐变进度条（约 3 秒，终点为球门），弹窗显示软件名与作者；
  服务就绪后自动打开浏览器，启动日志写入 `launcher.log`（不再刷屏终端）。

### 性能与内存
- TTS（edge-tts）与 Excel 解析（openpyxl）改为延迟加载，降低启动内存占用。
- 参考素材接口改为 SQL 查询，不再整表载入内存逐条过滤。
- SQLite 连接设置内存缓存上限（约 8MB）。
- TTS 缓存自动清理：超过 30 天的旧音频自动删除；总大小超过 200MB 时删除最旧文件（每天最多检查一次）。

### 修复/其他
- 深色模式低对比度问题持续收敛（本轮涉及启动链路相关样式）。
- 修复启动弹窗视觉：球门与进度条终点相连、消除左右空白；人物图换用带球素材（含右下角足球），
  解决此前「只有球」、黑底、以及图片路径错误导致的 ⚽ 兜底图标问题。
- 修复桌面快捷方式指向 KTRT.bat 导致的终端闪烁（改为直连 pythonw.exe）。
- README 更新：v0.1.1 安装包、无终端启动说明、考研词库。

## v0.1.0（2026-08-17）— 首个正式发布
- 两个安装包：KTRTSetup（完整版，内置 GRE必背）与 KTRTSetup-lite（纯净版，不含词库）。
- GRE 与雅思词库作为独立附件。
- 功能：一页一词、进度、熟悉/不熟悉/收藏、AI 自定义造句（每词最多 3 句）、离线词典、TTS 朗读、深色模式、多格式导入。



