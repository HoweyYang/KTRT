# KTRT 更新日志（Changelog）

## v0.2.0c（2026-09-17）— 修数据丢失与补丁半成品

- **修复（重要）**：重复导入同名词书不再清空学习记录。原先写词条用 `INSERT OR REPLACE`，SQLite 的 REPLACE 是"先删旧行再插新行"，外键级联会把「已背 / 收藏 / 不熟悉」、造句、错题、逐词笔记一起删掉，和界面「保留个人状态」的承诺正好相反；现改为 UPSERT 原地更新，词条内容照旧覆盖、行 id 不变，个人状态留住。
- **修复**：热补丁改为原子换入。先把补丁整包解压到临时目录并逐文件校验，全部通过才替换正式目录；此前是边解压边往正式目录写，一旦中途某个文件校验失败，就会留下「新 index.html + 旧 app.js」这种半成品，页面直接坏掉且不会回滚。
- **修复**：补丁应用后不再反复提示。此前只要 Release 上挂着补丁就一直显示「可更新」，应用完刷新仍在；现在补丁版本要新于已装补丁（没装过则比当前程序版本）才提示。
- **修复**：候选端口全被占用时不再回到已占用的端口。此前 8000–8019 全被占用会返回已占用的 8000，服务绑定失败却仍打开浏览器；现在改由系统分配一个空闲端口。
- **修复**：本地接口不再对任意网页敞开。去掉 CORS 通配并拦截非本机来源的请求，设置接口只回「有没有配过 Key」而不回明文。
- **优化**：「词书资源」的说法改为「开发者整理好的词书下载源」，不再自称"官方词书"。
- **测试**：新增 `tests/smoke_test.py`（接口、安全边界、Key 保存/清除、端口处理、启动器端到端）与 `tests/db_test.py`（重复导入保留状态、补丁原子性、补丁提示规则），共 48 项断言。

## v0.2.0b（2026-09-17）— 更新独立成页 + 一键更新

- **「更新」从设置页独立出来**：侧栏新增「更新」（刷新图标），与「设置」并列；设置页只保留 AI / 语音 / 主题。
- **补丁通道（热更新）**：Release 里挂 `patch-<版本>.zip` 时可在程序内一键应用——解压到数据目录 `web/`，优先于内置前端（无需重启、不需要管理员权限），刷新页面即生效；原补丁自动备份，可回退。
- **版本通道（一键装新版）**：检查到新版本可一键下载安装包并静默安装（`/SILENT /CLOSEAPPLICATIONS`），装完由独立脚本自动重开程序；学习数据在 `%APPDATA%\KTRT`，更新过程不触碰。首次会弹一次系统授权框。
- **启动自动检查（默认开）**：有新版或补丁时开程序弹窗提示，可选「稍后」或「本版本不再提醒」；可在更新页关掉自动检查。
- **更新检查不再依赖 GitHub API**（未认证会被限流 403）：改为读 `releases.atom` 拿最新 tag，再读 Release 的资产列表片段取安装包 / 补丁文件名与大小，只走 github.com。
- **发布补丁**：发新版时跑 `python tools/make_patch.py --version <该版本号>`，生成的 `release/patch-<版本号>.zip`（整套前端 + sha256 校验表）与安装包一起挂到 Release 即可。
- **修复**：静态文件查找按当前 Starlette 的返回约定重写（此前热补丁层会让 `/static/*` 全部 404）。

## v0.2.0a（2026-09-15）— 🎉 首个正式版
- **本版为第一个正式版**；0.1.0 – 0.1.6b 为开源内测版本。软著登记版本 V1.0 与 GitHub 版本链并行。
- **主题体系重做**：三种页面质感 × 每种三套配色——简约（Apple / OpenAI 风：浅色 / 深色 / 石墨）、纸质（米白纸 / 牛皮纸 / 靛蓝纸，纸纹 + 纤维 + 边缘暗角，衬线阅读字体）、科幻（霓虹 / 酸黄 / 矩阵：赛博深蓝多色混搭底、酸柠檬黄绿、纯绿黑）；侧栏配色按钮名称随质感变化。
- **学词页**：搭配与短语合并为一栏、逐条成行（含本词优先 → 更短优先 → 字母序兜底），每条独立朗读键；朗读增加"正在播"反馈、可再次点击停止、失败有明确提示。
- **编辑**：卡片内直接改词条（单词 / 音标 / 释义 / 搭配 / 短语 / 同反义词 / 同根词），同步本地库并回写词书 Excel；原文件失效时自动落本地托管副本。
- **内置动词短语库**：2217 条 / 3214 义项（中文释义、例句中译、语域、学习价值），导入词书时按动词自动检索并写入 `phrasal_keys`（名词不命中），老词库启动自动回填；卡片下方「动词短语参考」显示中文并可展开全部义项。
- **查词**：去掉 Wiktionary（常超时且无中文），保留 Datamuse 在线联想（改为查询后自动加载）；新增中↔英自动机翻；词组 / 句子可加入词库且只存释义（类型 + 译文 + 场景 + 拆解）。
- **定位跳转**：风暴页与查词结果显示所属词书芯片，点击直达该词书对应 List 与序号；查词弹窗新增「⚡ 风暴词卡」入口（已有词卡就地预览，没有则跳风暴页生成）。
- **品牌**：字标为 KTRT + 溯源词斩（三种质感各有字体风格）；页面中心新增随配色切换的水印位（可放入自己的字标图）；侧栏底部署名改手写体且不再被裁切。
- **修复**：检查更新时版本号比较丢失 a/b 后缀；原形归一误伤（bus → bu）；风暴列表在位置数据缺失时误报"不在词书"；导入词书用的临时文件被删导致无法回写 Excel。
- **修复（朗读）**：主单词键此前无"正在播"反馈、不能再点停止，现与其它朗读键一致；同一句合成结果做内存缓存并在卡片加载时预取，点击即出声；连点会中止在途合成并停掉上一段播放，不再叠音；切换单词自动停止上一段。

## v0.1.6b（2026-09-14）— 词条编辑 + 词书资源
- 学词页新增「编辑」：词卡上音标右侧的铅笔图标，点开即可在卡片内改这一条（单词、音标、词性释义、搭配、短语、同义词、反义词、同根词），保存后同步本地数据库，并回写这本词书的 Excel 对应行；原文件失效（被移动/删除，或当年是上传导入的临时文件）时自动改为写本地托管副本 `词书目录/wordbooks/<书名>.xlsx`。
- 导入的 Excel 不再用完即删：导入时留一份到本地词书目录并记为来源，之后编辑才能落回文件。
- 「导入」页新增「词书资源」：GRE / 雅思 / 考研三本官方词书一键下载，另附仓库 `wordbooks/` 目录入口。
- 「导入」页新增「复制自制词书提示词」：把手上的单词书交给本地 AI，按 11 列【】格式整理成 Excel 再导入（提示词全文见 `frontend/static/docs/词书整理提示词.md`）。

## v0.1.6（2026-09-08）— 单词头脑风暴 + 智能查词 + 学习书签
- 单词头脑风暴：本地词库 + 免费开源在线词源（Wiktionary / Datamuse）+ AI，生成结构化词卡：百科词源、使用场景、释义、词形变化、搭配、词性专属介词搭配、短语、俚语/习语、衍生词、同义词（含语义区别）、反义词、近义词（含语义区别）、形似易混淆词；逐词判断易混词数量（不易混的 0-3 个，易混的完整列出），并做专有名词大小写自纠正（rome→Rome、paris→Paris）。
- 风暴词卡独立存储（storm_entries），与离线词书不冲突；侧栏新增「风暴词卡」页，学词页「⚡ 风暴」入口；支持单条/批量导出 Excel / Markdown。
- 智能查词：变形自动还原原形（phenomena→phenomenon，ECDICT 0: + 规则兜底）；本地拼写纠错（您是不是想搜…）；免费开源在线词源 + 「AI 整理」旁路开关。
- ECDICT 词形代码解析为中文（原形/复数/过去式/过去分词/第三人称单数/比较级/最高级），定义与释义换行正确渲染。
- 学习书签：单词卡内小书签图标一键留/去，顶部下拉直达；每本书独立记忆学习位置（List + 序号），切书自动恢复。
- UI：自定义查词典移出单词卡；笔记本与单词卡等高；纸质主题只保留在设置页，侧栏仍为浅/深/深蓝三键。

## v0.1.5（2026-09-05）— 主题两轴化 + 所见即所得笔记本 + 词库补全
- 主题改两轴：页面质感（普通 / 纸质）× 主题色（浅色 / 深色 / 深蓝）共 6 种组合；纸质带 SVG 纸纹噪点、纸页内高光与柔和投影、衬线阅读字体（浅米 / 深棕 / 蓝调纸感三套）；旧「纸质」主题自动迁移为 纸质+浅色。
- 笔记本重做：移到单词卡右侧（约单词区一半宽，可随页面滚动）；取消「代码 / 展示」双标签，改为单层所见即所得编辑——原生 Markdown 输入即渲染（行首 `#`/`##` 自动变标题），`Ctrl+B` / `Ctrl+I` 加粗斜体，选中文字右键高亮 / 取消高亮，粘贴转纯文本；保存时自动转回 Markdown。
- 学词页左右分栏（单词主体 : 笔记本 ≈ 2:1），主区收窄防横向过长；侧栏可收缩成图标窄栏并记忆状态。
- 修复「检查更新」无限加载：并发请求 + 自动识别系统代理（Clash 等），无网时明确报错不再转圈。
- 雅思词汇真经：清理源文件自带水印广告（「需要完整表格加微信…」共 58 处）；「搭配」「短语」共 14352 条逐条补中文翻译（英文在前、中文在后，GRE 格式），数据库同步回写。
- 考研英语词汇词根+联想记忆法：「搭配」「短语」未含中文的条目全部按 GRE 格式补中文（源书自带中文的条目原样保留），数据库同步回写。
- 新增可复用词库补全工具 `tools/enrich_workbook.py`：自动清理水印 + 按 GRE 格式批量补「搭配/短语」中文翻译（短语级去重缓存、断点续跑），可一键回写 `data/ktrt.db`；以后任意 GRE 格式词书都能复用。

## v0.1.4a（2026-08-23）— 杀词闯关 + Codex 风格 UI + 新 logo
- 杀词：学习更名「学词」，新增「杀词」页：记词闯关与错题闯关。以 List 为最小单元、四选一匹配词性释义、每局乱序；答错自动标「不熟悉」并即时进入错题本；错题闯关可多选 List 合并题池，答对可移出错题本；记词闯关记录最高分；答题中进入专注模式（只剩题目 + 退出），切页清空本次进度（已入错题本保留）。
- UI 重构：顶部栏改为左侧栏（左上 KTRT wordmark、左下品牌与开发者署名），整体 Codex 风格；三主题（浅色 / 深色 / 深蓝高对比）侧边栏一键切换；图标统一为单色线条 SVG。
- 新 logo：豆包生成 + 几何定稿（实心 K、中心空心菱形），已替换应用、favicon、开发者头像与安装包图标（含 .ico）。
- 操作指南独立成页：三步上手 + 文档链接（不含 API Key 的使用说明 PDF、AI 扒取词书方法 PDF）+ 避坑提示。
- 其他：导入页说明精简；设置页新增「开发者」区（Howey Yueng）；错题勾选使用 WL 格式；清理历史孤立词条（已备份）；前端版本比较兼容 a/b 后缀（如 v0.1.4a）。

## v0.1.3（2026-08-20）— 自定义查词典 + 单词笔记本 + 体验优化
- 自定义查词典：学习页新增入口，支持 ① 离线词典速查 ② 词在已导入词书时直接「收藏」③ 词不在任何词书时用 AI 按标准范式生成，加入默认「外部单词收藏册」（每满 50 词换一个 List）。
- 默认词书调整：「外部单词收藏册」成为默认书（空书也常驻）；GRE必背 不再默认导入；词库统一放仓库 `wordbooks/` 目录按需下载。
- 单词笔记本：每个词可写 Markdown 笔记（`#`/`##` 两级标题、`**加粗**`、`==高亮==`，无斜体），代码 / 展示双视图，展示层支持框选右键高亮 / 取消高亮；显式「保存」按钮，未保存草稿切换即丢弃；管理页支持「有笔记」筛选、按词书/List 清空笔记、导出笔记（词 + 笔记循环）。
- 导出优化：词汇导出先选范围（不熟悉 / 收藏 / 一并），再按词书与 List 过滤导出。
- UI 与交互：移除「熟悉」标记（学习页与管理页）；管理页单词默认折叠（点 List 三角展开）；移除冗余「刷新」按钮；刷新页面保持当前所在页；「收藏单词册」更名为「外部单词收藏册」。
- 数据整理：清理历史遗留的孤立词条（备份已归档到 E 盘）。

## v0.1.2（2026-08-19）— 深色蓝默认主题 + 语音设置 + 使用须知
- 默认界面主题改为「深色 · 蓝」（设置里仍可选 浅色 / 深色蓝 / 深色高对比）。
- 语音升级：英语支持 美音/英音 × 男声/女声 四选，法语支持 男声/女声；新增 语速 / 音调 / 音量 调节（edge-tts 与浏览器语音均生效）。
- 修复主词朗读多读「音标」的问题（TTS 文本不再拼接音标）；朗读自动剔除中文翻译与全角括号，避免英文语音读中文。
- 设置页新增「使用须知 · 避坑指南」：常见崩溃原因、备份与恢复建议。
- GRE必背同根词全面修正（本地词库）：按「同根词 = 与目标词共享同一词根」标准重排；同义词的派生词回迁至同义词列并标注为 `原词（变形）`（如 abridge（abridgement, abridger）），删除重复与无关词，不足 2 个的词由 AI 按新标准补全并清洗生造词；修正后 6174 词拥有 1-4 个可靠同根词。词库数据在本机 data/（不入库），Release 附件待后续发布时同步。
- 软件更新：设置页新增「检查补丁更新 / 检查版本更新」（GitHub Atom 源实现，无 API 限流），展示最新 commit 与 Release 及下载链接。
- AI 造句规则：提示词与单词无关时忽略提示词、基于该单词随机造句；提示词相关时允许使用同根词/词性变形（如 abbreviate → abbreviation）；前端高亮同步支持同根词与变形。
- 朗读优化：多条搭配/短语/同根词之间（；分隔）自动加入停顿，不再一口气读完；TTS 响应禁用缓存（no-store）并加时间戳参数，避免浏览器播放旧语音。
- 路线图更新：单词笔记本、软件更新入口（更新补丁/更新版本）、lite 版发布策略（无默认词书、资料库独立附件）、安装器三语（简中/繁中/英语）、README 重写计划；软著申请指南存放于本机 tools/（不入库）。

## v0.1.1b（2026-08-18）— 关键崩溃修复
- 修复启动弹窗（tkinter/Tcl）销毁时硬崩溃整个进程（tcl86t.dll、异常 0x80000003）导致服务中途退出、后续操作 failed to fetch 的问题。
- 启动弹窗改为独立子进程运行：其 Tcl 回收崩溃只影响弹窗本身，主服务不受影响；已运行时点击仍显示弹窗。
- 验证：主进程连续运行稳定、端口持续监听、导出/收藏/管理接口正常。
## v0.1.1（2026-08-18）— 词库增强 + 体验与性能优化

### 词库
- GRE必背（6519 词）：全部单词的「搭配」「短语」加入地道中文翻译（英文在前、中文在后）；
  2130 个动词补充常用短语动词（含中文释义，区分字面直译与真实含义）；12 条数量不匹配词条定向修正。
- 新增考研英语词库附件 `KAOYAN_Wordbook.xlsx`（考研英语词汇词根+联想记忆法，5905 词，52 个 List），与 GRE、雅思并列。
- 三本词库均作为 Release 独立附件，可按需下载导入。

### 导出词汇
- 管理页新增「导出词汇」：可按「不熟悉」「收藏」单独或一并导出；
  导出为 Excel，内容为每个词在词库中的完整一行（单词 / 音标 / 词性释义 / 搭配 / 短语 / 同义词 / 反义词 / 同根词 / List / 语言 / 单词书），可直接再次导入。

### 启动体验
- 双击启动不再弹出终端窗口（打包版 `--noconsole`，源码版提供 `KTRT.vbs` 无终端入口）。
- 新增启动预备弹窗：带球小人 + 绿色渐变进度条（约 3 秒，终点为球门），弹窗显示软件名与作者；
  服务就绪后自动打开浏览器，启动日志写入 `launcher.log`（不再刷屏终端）。

### 性能与内存
- TTS（edge-tts）与 Excel 解析（openpyxl）改为延迟加载，降低启动内存占用。
- 参考素材接口改为 SQL 查询，不再整表载入内存逐条过滤。
- SQLite 连接设置内存缓存上限（约 8MB）。
- TTS 缓存自动清理：超过 30 天的旧音频自动删除；总大小超过 200MB 时删除最旧文件（每天最多检查一次）。

### 修复/其他
- 深色模式低对比度问题持续收敛（本轮涉及启动链路相关样式）。
- 修复启动弹窗视觉：球门与进度条终点相连、消除左右空白；人物图换用带球素材（含右下角足球），
  解决此前「只有球」、黑底、以及图片路径错误导致的 ⚽ 兜底图标问题。
- 修复桌面快捷方式指向 KTRT.bat 导致的终端闪烁（改为直连 pythonw.exe）。
- README 更新：v0.1.1 安装包、无终端启动说明、考研词库。

## v0.1.0（2026-08-17）— 首个正式发布
- 两个安装包：KTRTSetup（完整版，内置 GRE必背）与 KTRTSetup-lite（纯净版，不含词库）。
- GRE 与雅思词库作为独立附件。
- 功能：一页一词、进度、熟悉/不熟悉/收藏、AI 自定义造句（每词最多 3 句）、离线词典、TTS 朗读、深色模式、多格式导入。



